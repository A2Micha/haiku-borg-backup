PORT=8081
APP_SECRET=replace-with-a-long-random-secret
MOCK_BORG=0
TZ=Europe/Berlin
# Host directory exposed read-only inside the backend as /host.
# Use / for full-host backup access, or choose a narrower path such as /home.
HOST_ROOT=/
